[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --tb=short
    --cov=sqlite_utils_sqlite_graph
    --cov-report=term-missing
    --cov-report=html
